import styled from "styled-components";

const Select = styled.select`
    padding: 10px 20px;
    border: 2px solid #2c659b;
    border-radius: 4px;
    outline: none;

    cursor: pointer;

    transition: all 200ms;
`;

export default Select;